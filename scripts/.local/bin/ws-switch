#!/bin/sh
# WM-agnostic workspace switch used by the eww bar onclick (sway or i3).
if [ -n "${SWAYSOCK:-}" ]; then
    exec swaymsg workspace number "$1"
else
    exec i3-msg workspace number "$1"
fi
