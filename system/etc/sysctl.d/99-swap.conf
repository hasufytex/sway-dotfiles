# Swap is a fallback for memory pressure, not routine paging. Keep the kernel on
# RAM until it's nearly full, then spill to /swapfile (see swapfile.swap unit).
vm.swappiness = 10
vm.vfs_cache_pressure = 50
