# install: setup.sh installs this to /etc/systemd/system/adguardhome.service.d/ — waits for DHCP-assigned IP before binding, retries on transient failure
[Unit]
Wants=network-online.target
After=network-online.target

[Service]
Restart=on-failure
RestartSec=5
